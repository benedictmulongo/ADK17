package com.company;
import java.math.*;
import java.util.logging.XMLFormatter;

/**
 * Created by ben on 2017-09-07.
 */

// suppose that I  is a database wiht I = <key, position>
public class lazy_man
{
/*    int lazy(String w)
    {
        // W -> a string  "X1X2X3Y1Y2Y3"
        // w_prefix is a substring of W fom index 0 to 2, "X1X2X3"
        int not_found = -1000;

        String w_prefix = w.substring(0,2);
        int j,i,m;
        i = A[hash(wprfefix)];
        j = A[hash(wprfefix + 1)];

        while( j - i > 1000)
        {
            m = (int)Math.floor((i + j)/2);
            //got to position m in the index file I
            int pointer  = I.seek[m];
            //read the next word from I in s
            s = I[m +1];

            if(s <= m)
                i = m;
            else
                j = m;
            //got to position i in the index file I
            int pointer  = I.seek[i];
            while(true)
                //read the next word from I in s
                s = I[i +1];
                if(s = w)
                    //läs position in  x
                   x = read.position;
                   return x;
                if(s > w)
                    return not_found;

        }
    }*/
}

